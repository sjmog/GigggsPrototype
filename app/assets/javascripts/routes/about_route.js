//we've chosen to make the about section dynamic as it aids in maintaining the model in user memory, so filters etc. are not lost. This might change: it's a bit creepy that we could theoretically see what users are doing in their private sections. We don't want that, and we'll tell anyone clearly if that ever changes.
App.AboutRoute = Ember.Route.extend({
	
});
