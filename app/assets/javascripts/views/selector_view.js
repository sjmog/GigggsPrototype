App.SelectorView = Ember.View.extend({
	didInsertElement: function() {
		
		console.log('the selectorviews controller is now ' + this.controller + '**********')
	},
	
	controller: App.SelectorController,
	
	
	
	
}) 